<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">ManufacturingManager.Web</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        
        @* <div class="nav-item px-3"> *@
        @*     <NavLink class="nav-link" href="DimensionWorkload"> *@
        @*         <span class="oi oi-list-rich" aria-hidden="true"></span> Dimension Viewer *@
        @*     </NavLink> *@
        @* </div> *@
        @* *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="SolarInspectionsWorkload">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Solar Viewer
            </NavLink>
        </div>
        @* *@
        @* <div class="nav-item px-3"> *@
        @*     <NavLink class="nav-link" href="InprocessCheckWorkload"> *@
        @*         <span class="oi oi-list-rich" aria-hidden="true"></span> InProcess Check Viewer *@
        @*     </NavLink> *@
        @* </div> *@

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="FinalInspectionsWorkload">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Final Inspections Check Viewer
            </NavLink>
        </div>
        
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}