@page "/EditPriority"
@using PCSCaseManagement.Web.Data
@inject IPriorityRepository Repository 
@inject ProtectedSessionStorage SessionStorage
@inject NavigationManager NavigationManager
@inject UserLogged  CurrentUser 
@rendermode InteractiveServer
<PageTitle>Edit priority</PageTitle>
@attribute [Authorize(Roles = "Administrator")]
<div class="card border">
    <div class="card-header">
        <div class="row">
            <div class="col" tabindex="0">
                <h2 class="text-primary " id="startContent">@_addEditTitle</h2>
            </div>
        </div>

    </div>
    <div class="card-body">
        <FormLookUpTables LookUpTable="_priority" OnValidSubmit="Save" OnReturnToList="ReturnToList"/>
    </div>
</div>

@if (_showMessageBox)
{
 
    <MessageBox MessageBoxParameters="@_messageBoxParameters" OnClose="@MessageBoxClose" />
}
